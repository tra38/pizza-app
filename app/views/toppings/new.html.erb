<h2>Create New Topping</h2>
<%= form_for(@new_topping) do |f| %>
  <% if @new_topping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_topping.errors.count, "error") %> prohibited this pizza from being saved:</h2>

      <ul>
      <% @new_topping.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit 'Create Topping', :class => 'btn btn-primary' %>
<% end %>
<%= link_to 'Back', pizzas_path %>